describe('DialogCtrl', function() {
  beforeEach(angular.mock.module('Braniac'));

  var $controller;

  beforeEach(inject(function($controller) {
    $controller = $controller;
  }));

  describe('DialogCtrl check', function() {
    var controller;

    beforeEach(function() {
      controller = $controller('DialogCtrl');
    });

  });

});